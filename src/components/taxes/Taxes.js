import React from 'react';
import { View, Text } from 'react-native';
import PropTypes from 'prop-types';


const Taxes = (props) => (
  <View>
    <Text>Taxes</Text>
  </View>
);

Taxes.propTypes = { // eslint-disable-line

};

export default Taxes;
